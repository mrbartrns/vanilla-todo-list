import App from '@/App';
import './index.css';

const $root = document.querySelector('#root')!!;

// eslint-disable-next-line @typescript-eslint/no-unused-vars
new App({ $target: $root, initialState: null });
